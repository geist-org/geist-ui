import { Layout, Playground, Attributes } from 'lib/components'
import { Spinner, Spacer, Note, Code, Link } from 'components'
import NextLink from 'next/link'

export const meta = {
  title: 'Spinner',
  group: 'Feedback',
}

## Spinner

Indicate an action running in the background.

<Note>
  Looking for <Code>Loading</Code>? Use the{' '}
  <NextLink href="/en-us/components/loading">
    <a>Loading Component</a>
  </NextLink>
  .
</Note>

<Playground
  scope={{ Spinner }}
  code={`
<Spinner />
`}
/>

<Attributes edit="/pages/en-us/components/spinner.mdx">
<Attributes.Title>Spinner.Props</Attributes.Title>

| Attribute | Description  | Type             | Accepted values          | Default |
| --------- | ------------ | ---------------- | ------------------------ | ------- |
| ...       | native props | `HTMLAttributes` | `'id', 'className', ...` | -       |

</Attributes>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
