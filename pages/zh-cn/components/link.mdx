import { ExternalLink, Link as LinkIcon1, Link2 as LinkIcon2 } from '@zeit-ui/react-icons'
import { Layout, Playground, Attributes } from 'lib/components'
import { Link, Text, Code, Spacer } from 'components'
import NextLink from 'next/link'

export const meta = {
  title: '链接 Link',
  group: '导航',
}

## Link / 链接

网页之间的超链接。

<Playground
  desc="默认的链接样式。"
  scope={{ Link }}
  code={`
<Link href="#">气宇轩昂的超链接</Link>
`}
/>

<Playground
  title="朴素"
  desc="朴素的链接样式，默认颜色和正文一致。"
  scope={{ Link }}
  code={`
<Link href="#" plain>气宇轩昂的超链接</Link>
`}
/>

<Playground
  title="下划线"
  desc="链接显示下划线。"
  scope={{ Link }}
  code={`
<Link href="#" underline>气宇轩昂的超链接</Link>
`}
/>

<Playground
  title="图标"
  desc="在链接的左侧或者右侧显示自定义的修饰图标。"
  scope={{ Link, Text, ExternalLink, LinkIcon1, LinkIcon2 }}
  code={`
<>
  <Text>
    <Link href="#" iconRight={<LinkIcon1 />}>
      气宇轩昂的超链接
    </Link>
  </Text>
  <Text>
    <Link href="#" iconLeft={<LinkIcon2 />}>
      气宇轩昂的超链接
    </Link>
  </Text>
  <Text>
    <Link href="#" iconLeft={<LinkIcon2 />} iconRight={<ExternalLink />}>
      气宇轩昂的超链接
    </Link>
  </Text>
</>
`}
/>

<Playground
  title="块级链接"
  desc="将链接单独作为一个块显示。"
  scope={{ Link }}
  code={`
<Link href="#" block>气宇轩昂的超链接</Link>
`}
/>

<Playground
  title={
    <>
      与&nbsp;<Code>next/link</Code>&nbsp;组合
    </>
  }
  desc="使用 `Link` 组件与 NextJS 组合使用不需要任何额外的配置。"
  scope={{ Link, NextLink }}
  code={`
<NextLink href="/zh-cn/components/button">
  <Link block>气宇轩昂的超链接</Link>
</NextLink>
`}
/>

<Attributes edit="/pages/zh-cn/components/link.mdx">
<Attributes.Title>Link.Props</Attributes.Title>

| 属性          | 描述                                 | 类型                                            | 推荐值          | 默认    |
| ------------- | ------------------------------------ | ----------------------------------------------- | --------------- | ------- |
| **href**      | 链接地址                             | `string`                                        | -               | -       |
| **plain**     | 是否显示朴素样式，默认颜色和正文一致 | `boolean`                                       | -               | `false` |
| **iconLeft**  | 在左侧显示图标                       | `React.ReactNode`                               | -               | -       |
| **iconRight** | 在右侧显示图标                       | `React.ReactNode`                               | -               | -       |
| **underline** | 是否显示下划线                       | `boolean`                                       | -               | `false` |
| **block**     | 是否以块的方式显示链接               | `boolean`                                       | -               | `false` |
| **disabled**  | 是否被禁用                           | `boolean`                                       | -               | `false` |
| **ref**       | 转发的原生链接 Ref                   | <Code>Ref<HTMLAnchorElement &#124; null></Code> | -               | -       |
| ...           | 原生属性                             | `AnchorHTMLAttributes`                          | `'target', ...` | -       |

</Attributes>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
