import { Layout, Playground, Attributes } from 'lib/components'
import AlertCircle from '@zeit-ui/react-icons/alertCircle'
import { Spacer, Button, Code, Text, Dot } from 'components'
import  Input, { useInputHandle } from 'components/input'
import { useState, useEffect } from 'react'

export const meta = {
  title: '输入框 Input',
  group: '数据录入',
}

## Input / 输入框

处理用户的输入数据。

<Playground
  desc="基础的输入字段。"
  scope={{ Input, Spacer }}
  code={`
<>
  <Input placeholder="一个基础示例" />
  <Spacer inline />
  <Input variant="solid" placeholder="一个基础示例" />
</>
`}
/>

<Playground
  title="大小"
  desc="不同大小的输入框。"
  scope={{ Input, Spacer }}
  code={`
<>
  <Input size="mini" placeholder="mini" />
  <Spacer inline />
  <Input variant="solid" size="mini" placeholder="mini" />
  <Spacer y={.5} />
  <Input size="small" placeholder="small" />
  <Spacer inline />
  <Input variant="solid" size="small" placeholder="small" />
  <Spacer y={.5} />
  <Input size="medium" placeholder="medium" />
  <Spacer inline />
  <Input variant="solid" size="medium" placeholder="medium" />
  <Spacer y={.5} />
  <Input size="large" placeholder="large" />
  <Spacer inline />
  <Input variant="solid" size="large" placeholder="large" />
</>
`}
/>

<Playground
  title="宽度"
  desc="手动地设置宽度，或其他样式。"
  scope={{ Input }}
  code={`
<Input placeholder="一个基础示例" width="100%" />
`}
/>

<Playground
  title="不可交互地"
  desc="禁用输入框地所有交互效果。"
  scope={{ Input, Spacer }}
  code={`
<>
  <Input disabled placeholder="禁用" />
  <Spacer inline />
  <Input variant="solid" disabled placeholder="禁用" />
  <Spacer y={.5} />
  <Input readOnly defaultValue="只读" />
  <Spacer inline />
  <Input variant="solid" readOnly defaultValue="只读" />
</>
`}
/>

<Playground
  title="行内标签"
  desc="在输入框内显示短小地行内文本。"
  scope={{ Input, Spacer }}
  code={`
<>
  <Input id="username-1" label="用户名" placeholder="一个基础示例" />
  <Spacer inline />
  <Input id="username-2" variant="solid" label="用户名" placeholder="一个基础示例" />
  <Spacer y={.5} />
  <Input id="site-1" labelRight=".com" placeholder="https://github" />
  <Spacer inline />
  <Input id="site-2" variant="solid" labelRight=".com" placeholder="https://github" />
</>
`}
/>

<Playground
  title="块级标签"
  desc="为标签自定义更多样式。"
  scope={{ Input, Spacer, Text, Code, Dot }}
  code={`
<>
  <Input id="username-3" placeholder="小维特">
    用户名
  </Input>
  <Spacer />
  <Input id="title-1" placeholder="文章标题" width="50%">
    <Text h3>标题</Text>
  </Input>
  <Spacer />
  <Input id="title-2" placeholder="GitHub Actions" width="50%">
    <Dot type="warning">
      <Text small><Code>部署</Code> 问题详情</Text>
    </Dot>
  </Input>
</>
`}
/>

<Playground
  title="状态"
  desc="用色彩区分不同的状态。"
  scope={{ Input, Spacer }}
  code={`
<>
  <Input color="primary" defaultValue="主要" />
  <Spacer inline />
  <Input variant="solid" color="primary" defaultValue="主要" />
  <Spacer y={.5} />
  <Input color="error" defaultValue="包含错误" />
  <Spacer inline />
  <Input variant="solid" color="error" defaultValue="包含错误" />
</>
`}
/>

<Playground
  title="图标"
  desc="指定一个图标组件。"
  scope={{ Input, AlertCircle, Spacer }}
  code={`
<>
  <Input size="mini" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="mini" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="small" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="small" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="medium" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="medium" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="large" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="large" icon={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="mini" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="mini" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="small" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="small" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="medium" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="medium" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer y={.5} />
  <Input size="large" iconRight={<AlertCircle />} placeholder="获取信息" />
  <Spacer inline />
  <Input variant="solid" size="large" iconRight={<AlertCircle />} placeholder="获取信息" />
</>
`}
/>

<Playground
  title="密码"
  desc="显示或隐藏密码文本。"
  scope={{ Input }}
  code={`
<Input.Password defaultValue="123456abc" />
`}
/>

<Playground
  title="清除按钮"
  desc="在输入框内增加一个用于清除文本的按钮。"
  scope={{ Input, Spacer }}
  code={`
<>
  <Input clearable defaultValue="示例文字" placeholder="输入文本" />
  <Spacer inline />
  <Input variant="solid" clearable defaultValue="示例文字" placeholder="输入文本" />
</>
`}
/>

<Playground
  title="变化事件"
  desc="捕获输入框的文本变化。"
  scope={{ Input, useState }}
  code={`
() => {
  const [value, setValue] = useState('')
  const handler = (e) => {
    setValue(e.target.value)
    console.log(e.target.value)
  }
  return (
    <Input value={value} onChange={handler} placeholder="一个基础示例" />
  )
}
`}
/>

<Playground
  title={
    <>
      <Code>useInputHandle</Code>
    </>
  }
  desc="使用 `hooks` 捕获输入框的文本变化。"
  scope={{ Input, useInputHandle, Button, Spacer, useState }}
  code={`
() => {
  const { ref, getValue, setValue, focus, blur } = useInputHandle()
  const [v, setV] = useState(getValue())
  return (
    <>
      <Input ref={ref} defaultValue="初始值" />
      <Spacer y={.5} />
      <Button auto type="secondary" size="small"
        onClick={() => setValue(Math.random().toString(32))}>随机设置值</Button>
      <Spacer y={.5} />
      <Button auto size="small"onClick={() => setValue()}>重置值</Button>
      <Spacer y={.5} />
      <Button auto size="small"onClick={() => setV(getValue())}>获取值: {v}</Button>
    </>
  )
}
`}
/>

<Attributes edit="/pages/zh-cn/components/input.mdx">
<Attributes.Title>Input.Props</Attributes.Title>

| 属性              | 描述                 | 类型                                           | 推荐值                                  | 默认      |
| ----------------- | -------------------- | ---------------------------------------------- | --------------------------------------- | --------- |
| **variant**       | 输入框的类型         | `string`                                       | [InputVariantTypes](#inputvarianttypes) | `line`    |
| **value**         | 命令式设定输入框的值 | `string`                                       | -                                       | -         |
| **defaultValue**  | 初始值               | `string`                                       | -                                       | -         |
| **placeholder**   | 占位文本             | `string`                                       | -                                       | -         |
| **size**          | 输入框大小           | `NormalSizes`                                  | [InputSizes](#inputsizes)               | `medium`  |
| **color**         | 输入框颜色           | `NormalTypes`                                  | [InputColor](#inputcolor)               | `default` |
| **readOnly**      | 是否设置输入框为只读 | `boolean`                                      | -                                       | `false`   |
| **disabled**      | 是否禁用输入框       | `boolean`                                      | -                                       | `false`   |
| **clearable**     | 是否展示清除按钮     | `boolean`                                      | -                                       | `false`   |
| **label**         | 文本标签             | `string`                                       | -                                       | -         |
| **icon**          | 输入框图标           | `React.ReactNode`                              | -                                       | -         |
| **labelRight**    | 居于右侧的文本标签   | `string`                                       | -                                       | -         |
| **iconRight**     | 居于右侧的图标       | `React.ReactNode`                              | -                                       | -         |
| **iconClickable** | 图标是否可点击       | `boolean`                                      | -                                       | `false`   |
| **onIconClick**   | 图标点击事件         | `(e: React.ChangeEvent) => void`               | -                                       | -         |
| **onChange**      | 输入框变化事件       | `(e: React.ChangeEvent) => void`               | -                                       | -         |
| **onClearClick**  | 清除按钮的点击事件   | `(e: React.MouseEvent) => void`                | -                                       | -         |
| **ref**           | 转发的原生输入框 Ref | <Code>Ref<HTMLInputElement &#124; null></Code> | -                                       | -         |
| ...               | 原生属性             | `InputHTMLAttributes`                          | `'id', 'className', ...`                | -         |

<Attributes.Title>Input.Password.Props</Attributes.Title>

| 属性           | 描述               | 类型          | 推荐值                      | 默认    |
| -------------- | ------------------ | ------------- | --------------------------- | ------- |
| **hideToggle** | 隐藏切换密码的按钮 | `boolean`     | -                           | `false` |
| ...            | 输入框组件属性     | `Input.Props` | [Input.Props](#input.props) | -       |

<Attributes.Title>InputVariantTypes</Attributes.Title>

```ts
type InputVariantTypes = 'solid' | 'line'
```

<Attributes.Title>InputSizes</Attributes.Title>

```ts
type InputSizes = 'mini' | 'small' | 'medium' | 'large'
```

<Attributes.Title>InputColor</Attributes.Title>

```ts
type InputColor = 'default' | 'secondary' | 'success' | 'warning' | 'error'
```

<Attributes.Title>useInputHandle</Attributes.Title>

```ts
type useInputHandle = () => {
  ref: React.RefObject<HTMLInputElement>
  setValue: (value: string = '') => void
  getValue: () => string | undefined
  focus: () => void
  blur: () => void
}
```

</Attributes>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
