import { Layout, Playground, Attributes } from 'lib/components'
import { Tabs, Spacer, Link, Text, Button, Code } from 'components'
import { useState } from 'react'
import TwitterIcon from '@zeit-ui/react-icons/twitter'
import TwitchIcon from '@zeit-ui/react-icons/twitch'

import Default from 'pages/code/tabs/default.playground'
import Solid from 'pages/code/tabs/solid.playground'
import Devider from 'pages/code/tabs/devider.playground'
import Dynamic from 'pages/code/tabs/dynamic.playground'
import Controlled from 'pages/code/tabs/controlled.playground'
import Imperative from 'pages/code/tabs/imperative.playground'

export const meta = {
  title: '选项卡 Tabs',
  group: '导航',
}

## 选项卡

展示选显卡内容

<Default />
<Solid />
<Devider />
<Dynamic />
<Controlled />
<Imperative />

<Attributes edit="/pages/zh-cn/components/tabs.mdx">
<Attributes.Title>Tabs.Props</Attributes.Title>

| Attribute        | Description                  | Type                    | Accepted values                 | Default |
| ---------------- | ---------------------------- | ----------------------- | ------------------------------- | ------- |
| **initialValue** | 初始活跃标签                 | `string`                | -                               | -       |
| **value**        | 当前状态, 决定是否是受控组件 | `string`                | -                               | -       |
| **showDivider**  | 显示分割线                   | `boolean`               | `true` or `false`               | -       |
| **onChange**     | 监听变化                     | `(val: string) => void` | -                               | -       |
| **varient**      | 选下卡展现形式               | `string`                | `solid` or `line`               | `line`  |
| ...              | 原生                         | `HTMLAttributes`        | `'alt', 'id', 'className', ...` | -       |

<br />
<strong>注意:</strong>
<br />

1. `value`用来判定该组件是否受控, 虽然我们不会报错，不过请不要在受控和非受控间切换

<Attributes.Title>Tabs.useTabsHandle</Attributes.Title>

| Attribute         | Description  | Accepted values | Default |
| ----------------- | ------------ | --------------- | ------- |
| **getCurrentTab** | 读取组件状态 | `void`          | -       |
| **setCurrentTab** | 改变组件状态 | `string`        | -       |

<br />
<strong>注意:</strong>

1. `ref` 只在非受控组件中可以使用, 也就是 `value` 没有传入的情况.`props` 是在 react 的数据流中的推荐用法.
   确定 `useTabsHandle` 真的有必要再使用

<br />

<Attributes.Title alias="Tabs.Tab">Tabs.Item.Props</Attributes.Title>

| Attribute            | Description   | Type                          | Accepted values | Default |
| -------------------- | ------------- | ----------------------------- | --------------- | ------- |
| **label**            | 显示 tab 标签 | `string` or `React.ReactNode` | -               | -       |
| **value** (required) | 定位标签      | `string`                      | -               | -       |
| **disabled**         | 时候 disable  | `boolean`                     | -               | `false` |

</Attributes>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
